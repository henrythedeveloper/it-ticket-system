    server {
        listen 80;
        server_name _;

        location / {
            # Return '204 No Content' for all requests to the root
            # This bypasses any proxying for testing purposes
            return 204;
        }

        # Keep the API location block commented out for now
        # location /api {
        #    proxy_pass http://it-helpdesk-backend:8080;
        #    proxy_set_header Host $host;
        #    proxy_set_header X-Real-IP $remote_addr;
        #    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #    proxy_set_header X-Forwarded-Proto $scheme;
        # }
    }
    